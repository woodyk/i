[pytest]
# Run tests in verbose mode for detailed output
addopts = -v

# Specify the directory where tests are located
testpaths = tests

# Automatically include the root directory in the Python path for imports
pythonpath = .

